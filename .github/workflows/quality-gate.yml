name: Quality Gate

on:
  pull_request:
    branches: [main, dev]
    paths:
      - 'src/**'
      - '.flake8'
      - 'src/backend/requirements.txt'
      - 'src/frontend/package.json'
      - 'src/frontend/package-lock.json'
      - '.github/workflows/quality-gate.yml'

permissions:
  contents: read

jobs:
  backend-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ruff/flake8 are static analyzers — the app's dependencies are not
      # needed to lint, and installing them here re-runs pip resolution for
      # no benefit (deps are exercised with the exact uv.lock in test.yml).
      - name: Install linters
        run: |
          python -m pip install --upgrade pip
          pip install ruff flake8

      - name: Ruff (unused imports + critical errors)
        run: ruff check src/backend --select F401,E9,F63,F7,F82

      - name: Flake8
        run: flake8 --config=.flake8 --exclude=tests src/backend

  frontend-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: src/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: src/frontend

      - name: ESLint
        run: npm run lint
        working-directory: src/frontend

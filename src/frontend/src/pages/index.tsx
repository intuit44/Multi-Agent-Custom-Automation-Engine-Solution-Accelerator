export { default as HomePage } from './HomePage';
export { default as ChatPage } from './ChatPage';
// PlanPage kept for reference but no longer a route — ChatPage handles both modes
export { default as PlanPage } from './PlanPage';
